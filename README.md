# JavaScript & React Workshops

นี่คือโค้ดสำหรับ Workshop ที่ได้รับมอบหมาย โดยแบ่งออกเป็น 2 ส่วน:
1.  **Workshops 3.1 - 3.5 (JavaScript พื้นฐาน)**
2.  **Workshops 5.1 - 5.2 (React Components)**

---

## 🚀 วิธีการรันโค้ด (ส่วนที่ 1: JavaScript พื้นฐาน)

โค้ดสำหรับ Workshop 3.1 - 3.5 อยู่ในไฟล์ `main.js`

คุณสามารถรันไฟล์นี้ได้ผ่าน 2 วิธีหลัก:

1.  **ผ่าน Node.js (แนะนำ):**
    ```bash
    node main.js
    ```
2.  **ผ่าน Console ใน Web Browser:**
    นำโค้ดทั้งหมดไปวางใน Console ของบราวเซอร์ (กด F12) เพื่อดูผลลัพธ์

### Workshop 3.1: การตัดเกรดของนักศึกษา

* **ฟังก์ชัน:** `getGrade(score)`
* **การทำงาน:** รับคะแนน (0-100) และใช้ `if` (ห้ามใช้ `else`) เพื่อพิมพ์เกรด ("A", "B", "C", "D", "F") ออกทาง Console

### Workshop 3.2: การคำนวณราคา

* **ฟังก์ชัน:** `calculatePrice(productPrice, discountPercentage)`
* **การทำงาน:** คำนวณราคาสินค้าหลังหักส่วนลด และตรวจสอบเงื่อนไขค่าจัดส่ง (บวก 50 ถ้าต่ำกว่า 500) หรือส่วนลดพิเศษ (ลด 10% ถ้าสูงกว่า 500)

### Workshop 3.3: สร้างโปรแกรมแสดงข้อมูลของนักศึกษา

* **ข้อมูล:** Array `students`
* **ฟังก์ชัน:**
    * `displayStudentInfo()`: ใช้ `forEach` วนลูปแสดงข้อมูลนักศึกษา
    * `showStudentCount()`: ใช้ `.length` แสดงจำนวนนักศึกษาทั้งหมด

### Workshop 3.4: การคำนวณผลรวมของราคาสินค้าพร้อมส่วนลด

* **ข้อมูล:** Array `exampleProducts`
* **ฟังก์ชัน:** `calculateTotal()`
* **การทำงาน:** ใช้ `forEach` วนลูป Array เพื่อคำนวณราคาสินค้าหลังหักส่วนลดของแต่ละชิ้น แล้วบวกรวมกันทั้งหมด พิมพ์ผลลัพธ์รวมออกทาง Console

### Workshop 3.5: สร้างโปรแกรมคัดกรองนักเรียนที่ผ่านเกณฑ์

* **ข้อมูล:** Array `studentsList`
* **ฟังก์ชัน:** `filterPassedStudents(passScore)`
* **การทำงาน:** รับค่า `passScore` (คะแนนขั้นต่ำ) แล้วใช้ `forEach` วนลูปเพื่อพิมพ์ชื่อนักเรียนที่มีคะแนนมากกว่าหรือเท่ากับเกณฑ์ที่กำหนด

---

## 🚀 วิธีการรันโค้ด (ส่วนที่ 2: React Workshops)

โค้ดสำหรับ Workshop 5.1 และ 5.2 อยู่ในไฟล์ `Counter.js` และ `UserForm.js` ตามลำดับ

**สำคัญ:** ไฟล์เหล่านี้เป็น React Components **ไม่สามารถรันได้โดยตรง**เหมือนไฟล์ `main.js`

คุณต้องนำไฟล์เหล่านี้ไปใส่ในโปรเจกต์ React ที่ถูกสร้างด้วย `create-react-app` หรือ `vite` เพื่อให้มันทำงานได้

### Workshop 5.1: Counter Component (ไฟล์ `Counter.js`)

* **Component:** `Counter`
* **การทำงาน:**
    * ใช้ `useState` เพื่อเก็บค่า `count` (เริ่มต้นที่ 0)
    * มีปุ่ม **"+ เพิ่ม"** (บวก 1), **"- ลด"** (ลบ 1), และ **"Reset"** (กลับไปเป็น 0)
    * แสดงค่า `count` ปัจจุบันบนหน้าจอ

### Workshop 5.2: Form Component (ไฟล์ `UserForm.js`)

* **Component:** `UserForm`
* **การทำงาน:**
    * ใช้ `useState` เพื่อเก็บข้อมูล `form` (name และ email)
    * มีช่อง Input 2 ช่องสำหรับ "ชื่อ" และ "อีเมล"
    * ข้อมูลที่กรอกจะแสดงผลแบบ Real-time ใต้ฟอร์ม
    * ปุ่ม **"Submit"** จะแสดง `alert` ข้อมูลที่กรอก
    * ปุ่ม **"Clear"** จะล้างข้อมูลในฟอร์มทั้งหมด
